{
  "name": "lighthouse",
  "version": "1.0.0",
  "description": "the best barber cms and booking system",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "npm": ">=10.4.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present",
    "dev": "npm run dev --workspace:server-api ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "eslintConfig": {
    "ignorePatterns": [
      "node_modules/",
      "dist/"
    ],
    "extends": "./node_modules/standard/eslintrc.json"

  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
